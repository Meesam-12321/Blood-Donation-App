name: donorfusion
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.1.1
  firebase_analytics: ^11.1.0
  firebase_auth: ^5.1.1
  cloud_firestore: ^5.0.2
  google_fonts: ^6.2.1
  flutter_animate: ^4.5.0 # Add this for animations
  provider: ^6.1.2
  shared_preferences: ^2.2.3
  firebase_database: ^11.0.2
  # Add other dependencies as needed

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
